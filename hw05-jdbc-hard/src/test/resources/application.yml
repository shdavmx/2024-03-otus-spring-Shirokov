application:
  title: hw05-spring-jdbc
  version: 1.0
spring:
  shell:
    script:
      enabled: true
    interactive:
      enabled: true
  datasource:
    url: jdbc:h2:mem:maindb
  sql:
    init:
      mode: always
      data-locations: data.sql
      schema-locations: schema.sql